# Pins that must survive installing Motus's dependencies into the RoboTwin conda env.
# curobo and pytorch3d are compiled against this exact torch ABI (CLAUDE.md 1.2/8), SAPIEN
# 3.0.0b1 imports pkg_resources (removed in setuptools >= 70), and the sim stack is pinned
# to numpy 1.x.  pip errors out instead of silently upgrading any of these.
torch==2.4.1
torchvision==0.19.1
numpy==1.26.4
setuptools==69.5.1
sapien==3.0.0b1
mplib==0.2.1
open3d==0.18.0
scipy==1.15.3
opencv-python==4.11.0.86
pillow==11.3.0
